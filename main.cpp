// Командный проект. Группа ПИ-53.
// Команда: Гацер (в. 82, техлид), Гудочкин (в. 14), Луканин (в. 36).
#include <iostream>
#include <windows.h>
// === БЛОК ПОДКЛЮЧЕНИЙ: каждый участник добавляет свой заголовочный файл ===
#include "gatser.h"
// #include "gudochkin.h"
// #include "lukanin.h"
// === КОНЕЦ БЛОКА ПОДКЛЮЧЕНИЙ ===
using namespace std;
int main() {
	SetConsoleCP(65001);
	SetConsoleOutputCP(65001);
	int choice, n;
	double P, r;
	do {
		cout << "\n=== Командный проект: сборник расчётов ===\n";
		// === БЛОК МЕНЮ: каждый участник добавляет свои пункты ===
		cout << "1. Итоговая сумма с учетом сложных процентов (наращенный капитал)\n";
		cout << "2. Сумма сложных процентов (чистый доход)\n";
		// === КОНЕЦ БЛОКА МЕНЮ ===
		cout << "0. Выход\n";
		cout << "Выберите пункт: ";
		cin >> choice;
		switch (choice) {
		// === БЛОК ОБРАБОТКИ: каждый участник добавляет свои case ===
		case 1:
			cout << "Введите первоначальную сумму P (руб): ";
			cin >> P;
			cout << "Введите процентную ставку r (% за месяц): ";
			cin >> r;
			cout << "Введите число месяцев n: ";
			cin >> n;
			cout << "Итоговая сумма с учетом сложных процентов = " << compoundTotal(P, r, n) << " руб.\n";
			break;
		case 2:
			cout << "Введите первоначальную сумму P (руб): ";
			cin >> P;
			cout << "Введите процентную ставку r (% за месяц): ";
			cin >> r;
			cout << "Введите число месяцев n: ";
			cin >> n;
			cout << "Сумма сложных процентов = " << compoundInterest(P, r, n) << " руб.\n";
			break;
		// === КОНЕЦ БЛОКА ОБРАБОТКИ ===
		case 0:
			cout << "Работа завершена.\n";
			break;
		default:
			cout << "Такого пункта нет.\n";
		}
	} while (choice != 0);
	return 0;
}
