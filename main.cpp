// Командный проект. Группа ПИ-53.
// Команда: Гацер (в. 82, техлид), Гудочкин (в. 14), Луканин (в. 36).
#include <iostream>
// === БЛОК ПОДКЛЮЧЕНИЙ: каждый участник добавляет свой заголовочный файл ===
// #include "gatser.h"
// #include "gudochkin.h"
#include "lukanin.h"
// === КОНЕЦ БЛОКА ПОДКЛЮЧЕНИЙ ===
using namespace std;
int main() {
	int choice;
	double k, x;
	do {
		cout << "\n=== Командный проект: сборник расчётов ===\n";
		// === БЛОК МЕНЮ: каждый участник добавляет свои пункты ===
		cout << "5. Сила упругости\n";
		cout << "6. Энергия упругой деформации\n";
		// === КОНЕЦ БЛОКА МЕНЮ ===
		cout << "0. Выход\n";
		cout << "Выберите пункт: ";
		cin >> choice;
		switch (choice) {
			// === БЛОК ОБРАБОТКИ: каждый участник добавляет свои case ===
		case 5:
			cout << "Введите значения k и x (в ньютонах и метрах соответственно): ";
			cin >> k >> x;
			cout << "Сила упругости = " << springForce(k, x) << "\n";
			break;
		case 6:
			cout << "Введите значения k и x (в ньютонах и метрах соответственно): ";
			cin >> k >> x;
			cout << "Энергия упругой деформации (в джоулях) = " << springEnergy(k, x) << "\n";
			break;
			// === КОНЕЦ БЛОКА ОБРАБОТКИ ===
		case 0:
			cout << "Работа завершена.\n";
			break;
		default:
			cout << "Такого пункта нет.\n";
		}
	} while (choice != 0);
	return 0;
}
